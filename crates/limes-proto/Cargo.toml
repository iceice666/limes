[package]
name = "limes-proto"
description = "Shared protocol/types for limes frontends and backend code."
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[lib]
path = "src/lib.rs"
